<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post a New Blog - Reverse OJ</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-red-100 font-sans leading-normal tracking-normal">
    <!-- Include Dashboard (Navbar) -->

    <!-- Page Header -->
    <header class="bg-red-600 text-white p-16 text-center">
      <h1 class="text-5xl font-bold">Post a New Blog</h1>
      <p class="mt-4 text-lg">
        Share your thoughts and insights with the community!
      </p>
    </header>

    <!-- Blog Posting Form -->
    <main class="container mx-auto mt-10 p-4">
      <section class="bg-white shadow-md rounded-lg p-6 mb-8">
        <h2 class="text-2xl font-bold text-red-600 mb-4">Create Your Blog</h2>
        <form action="/blogs" method="POST">
          <div class="mb-4">
            <label for="title" class="block text-gray-700">Title:</label>
            <input
              type="text"
              id="title"
              name="title"
              required
              class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-red-500 focus:ring focus:ring-red-500 focus:ring-opacity-50"
            />
          </div>
          <div class="mb-4">
            <label for="subtitle" class="block text-gray-700">Subtitle:</label>
            <input
              type="text"
              id="subtitle"
              name="subtitle"
              class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-red-500 focus:ring focus:ring-red-500 focus:ring-opacity-50"
            />
          </div>
          <div class="mb-4">
            <label for="content" class="block text-gray-700">Content:</label>
            <textarea
              id="content"
              name="content"
              rows="5"
              required
              class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-red-500 focus:ring focus:ring-red-500 focus:ring-opacity-50"
            ></textarea>
          </div>
          <div class="mb-4">
            <label for="created_by" class="block text-gray-700">Author:</label>
            <input
              type="text"
              id="created_by"
              name="created_by"
              required
              class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:border-red-500 focus:ring focus:ring-red-500 focus:ring-opacity-50"
            />
          </div>
          <button
            type="submit"
            class="bg-red-600 text-white font-bold py-2 px-4 rounded hover:bg-red-700"
          >
            Submit
          </button>
        </form>

        <!-- Display success message -->
      </section>
    </main>

    <!-- Include Footer -->
    <%- include('./footer') %>
  </body>
</html>
