<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Set Problem</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto mt-10">
      <h1 class="text-3xl font-bold text-center mb-5">Set New Problem</h1>

      <form
        action="/setProblem"
        method="POST"
        enctype="multipart/form-data"
        class="max-w-lg mx-auto bg-white p-8 rounded-lg shadow-md"
      >
        <div class="mb-4">
          <label
            for="problemName"
            class="block text-gray-700 text-sm font-bold mb-2"
            >Problem Name</label
          >
          <input
            type="text"
            id="problemName"
            name="problemName"
            class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            required
          />
        </div>

        <div class="mb-4">
          <label
            for="problemStatement"
            class="block text-gray-700 text-sm font-bold mb-2"
            >Problem Statement (PDF)</label
          >
          <input
            type="file"
            id="problemStatement"
            name="problemStatement"
            accept="application/pdf"
            class="block w-full text-sm text-gray-700 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none"
            required
          />
        </div>

        <div class="mb-4">
          <label
            for="inputFile"
            class="block text-gray-700 text-sm font-bold mb-2"
            >Input File (.txt)</label
          >
          <input
            type="file"
            id="inputFile"
            name="inputFile"
            accept=".txt"
            class="block w-full text-sm text-gray-700 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none"
            required
          />
        </div>

        <div class="mb-4">
          <label
            for="outputFile"
            class="block text-gray-700 text-sm font-bold mb-2"
            >Output File (.txt)</label
          >
          <input
            type="file"
            id="outputFile"
            name="outputFile"
            accept=".txt"
            class="block w-full text-sm text-gray-700 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none"
            required
          />
        </div>

        <div class="flex items-center justify-between">
          <button
            type="submit"
            class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
          >
            Upload Problem
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
