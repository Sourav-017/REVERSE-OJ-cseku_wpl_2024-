<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Reverse OJ</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-red-100">
    <div class="bg-red-700 p-6 text-white text-center">
      <h1 class="text-3xl font-bold">Reverse OJ</h1>
      <p>Insights, tips, and tutorials from Reverse OJ</p>
    </div>

    <div class="container mx-auto max-w-lg mt-12">
      <div class="bg-white p-8 rounded-lg shadow-md">
        <h3 class="text-2xl font-semibold text-red-700 mb-4">Login</h3>
        <!-- The action URL and method here will work with the POST request you set up in Express -->
        <form action="/auth/login" method="POST">
          <div class="mb-4">
            <label for="username" class="block text-gray-700 font-semibold mb-1"
              >Username</label
            >
            <input
              type="text"
              id="username"
              name="username"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-700"
            />
          </div>
          <div class="mb-6">
            <label for="password" class="block text-gray-700 font-semibold mb-1"
              >Password</label
            >
            <input
              type="password"
              id="password"
              name="password"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-700"
            />
          </div>
          <button
            type="submit"
            class="w-full py-2 bg-green-600 text-white font-bold rounded-md hover:bg-green-700 transition"
          >
            Login
          </button>
        </form>
        <div class="mt-4 text-center">
          <p class="text-gray-700">Don't have an account?</p>
          <a
            href="/auth/signup"
            class="text-blue-600 font-semibold hover:underline"
            >Sign up here</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
